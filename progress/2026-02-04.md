# 2026-02-04

## Phase 37 (Desktop: Bilingual Support - Design)

- Drafted design plan for Chinese/English support in the FileDock desktop app (React + Tauri), including i18n setup, settings storage, and UI string migration.

## Phase 38 (Desktop: Bilingual Support - Implementation)

- Added i18n scaffolding (i18next + react-i18next), locale resolution (auto/zh-CN/en), and HTML lang updates.
- Extended settings with `locale` and Preferences language selector (Auto/English/中文).
- Replaced UI strings with translation keys across App, command palette, workspace/panes, and queue/device/local/SFTP views.
- Added EN/ZH translation resources and updated tab title localization.

## Phase 39 (Dev: Vite Host Checks)

- Enabled Vite dev server host and allowedHosts to support tunnel access (commit 26dcf15).

## Phase 40 (Dev: Remote Access)

- Verified FileDock dev server running and provided a Cloudflare quick tunnel URL for remote access.

## Phase 41 (Desktop: Web Preview Guard)

- Added web preview fallback: Tauri API guards, dialog stubs, preview banner, and boot/error fallbacks to avoid blank screen in browser.

## Phase 42 (Desktop: UI Copy)

- Removed brand meta subtitle text ("desktop UI shell") from the header.

## Phase 43 (Desktop: Connection Help)

- Added a connection help panel explaining server URL/token/device fields in the top bar.

## Phase 44 (Desktop: Bug Fix)

- Fixed "Cannot access 't2' before initialization" when adding a new tab (variable shadowing).

## Phase 45 (Desktop: Workspace Button)

- Changed the new workspace button to show only "+" with hover title "新增工作区".

## Phase 46 (Desktop: Multi-View Layout + TUI)

- Restored split layout rendering with LeafPane/SplitNodeView, added right-side toolbar "add view" button (default Local), and enabled layout persistence for splits.
- Local file picker now shows a desktop-only notice in web preview; device refresh button simplified to an icon.

## Phase 47 (Desktop: TUI Styling Pass)

- Switched the UI to a more TUI-like look: monospace base, denser sizing, subtle gradients/patterns, and compact tabs/pane chrome.
