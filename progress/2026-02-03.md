# 2026-02-03

## Phase 28 (Desktop: Flat Theme Tokens - vben-inspired)

- Added semantic HSL design tokens (`background/foreground/muted/border/ring/primary/...`) and a FileDock "flat" theme preset (light + dark).
- Added theme application plumbing (`applyTheme`) and persisted theme preferences (mode/radius/font size).
- Kept compatibility with existing CSS by mapping legacy vars to semantic tokens.

## Phase 29 (Desktop: Preferences Modal - Theme Controls)

- Added Preferences modal (Ctrl/Cmd + ,) to control:
  - theme mode (light/dark/auto)
  - radius
  - base font size
- Added JSON export/import for settings (clipboard + prompt fallback).

## Phase 30 (Desktop UI: Flat Component Recipes + Panel Language)

- Standardized "field" recipe (inputs/select/textarea): border/bg/radius/height/padding/placeholder/focus.
- Standardized "button" recipe: consistent hover/active overlays across light/dark.
- Unified modal/panel layout recipe (header/body/footer) and softened internal separators (subtle strokes).

## Phase 31 (Desktop UI: Interaction Recipes)

- Unified tab + list item hover/active/selected states under a shared token-driven recipe.
- Added selection highlight for device tree rows.

## Phase 32 (Desktop UI: Command Palette Wiring)

- Wired Command Palette overlay with Ctrl/⌘ + K and app-level commands (prefs, new tab, switch workspace, theme toggle).
- Added command palette styling + field recipe integration.

Next:
- Optional: expand Command Palette actions (pane splits, transfer queue ops) and improve fuzzy matching.
