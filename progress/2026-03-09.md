# 2026-03-09

## P11 guided agent onboarding planning

- Wrote a dedicated planning doc for the next FileDock milestone in `docs/agent-onboarding.md`.
- Turned the vague roadmap item into concrete scope, requirements, acceptance criteria, technical design, milestone slices, and open questions.
- Grounded the plan in the current implementation: existing CLI agent runtime, device registration API, heartbeat support, config export/QR payloads, desktop config import, and platform service templates.
- Updated `docs/roadmap.md` to reflect that config export/import baseline already exists and that the remaining near-term work is the guided onboarding flow (register device, persist profile, install service, verify status).

## Validation

- `git diff --check`


## P12 CLI-first agent onboarding slice

- Added first-class CLI lifecycle commands for the onboarding milestone:
  - `filedock device register`
  - `filedock device heartbeat`
  - `filedock agent init`
  - `filedock agent install`
  - `filedock agent status`
  - `filedock agent uninstall`
- Kept backward compatibility for the existing runtime entrypoint: `filedock agent --config ...` still works, and `filedock agent run --config ...` is also available.
- Added platform-default profile/state path resolution, config JSON import support for `agent init`, automatic device registration when credentials are missing, and best-effort service installers/status probes for Linux/macOS/Windows.
- Updated agent docs / quickstart to show the new profile-based setup flow instead of only manual TOML editing.

## Validation

- `cargo test -p filedock`
- `cargo run -p filedock -- device --help`
- `cargo run -p filedock -- agent --help`
- `cargo run -p filedock -- agent init --help`
- Temp-home smoke: `agent init --no-register`, `agent install --dry-run`, `agent status`

## P13 desktop onboarding wizard slice

- Added a dedicated desktop `Set up agent` entry point (toolbar + Preferences) instead of hiding onboarding behind raw JSON import.
- Built a guided modal around the new CLI lifecycle: paste/import server payload, choose profile/device/folder, set intervals, preview auth persistence, create the saved profile, preview/install the platform service, and refresh verification status.
- Added new Tauri bridge commands that wrap `filedock agent init`, `filedock agent install`, and `filedock agent status`, so the desktop app stays a wizard while the CLI remains the persistent worker.
- Added unit coverage for profile/auth preview helpers and updated desktop/agent docs plus roadmap wording to reflect the new setup flow.

## Validation

- `cd apps/desktop && npm test`
- `cd apps/desktop && npm run build`
- `cargo check -p filedock-desktop`
- `git diff --check`

## P14 desktop onboarding cleanup controls

- Extended the desktop onboarding wizard with a cleanup path: the UI can now uninstall the platform background service and optionally delete the saved profile TOML.
- Added the matching Tauri bridge / typed frontend API for `filedock agent uninstall`, surfaced cleanup results in the wizard, and updated agent + desktop docs to include the removal flow.

## Validation

- `cd apps/desktop && npm test`
- `cd apps/desktop && npm run build`
- `cargo check -p filedock-desktop`
- `git diff --check`
