# 2026-03-06

## New Requirement (from Nita)

- Add support for right-click one-click backup to FileDock service.
- Target interaction: in file/folder context menu, user can trigger "Backup to FileDock" directly.

- Support saving current window as a fixed node preset and showing saved nodes in a dropdown for quick selection.
- When saving a node preset, require a user-provided node name (mandatory).
- Add server status display for FileDock service in the UI.
- UI optimization: use icon + tooltip style buttons instead of plain text-only buttons.
- UI optimization: support adjustable pane column widths (drag to resize).
- Bug report: delete-with-backup flow fails because push-folder create snapshot returns HTTP 502 at /v1/snapshots.
- Add a dedicated "New Terminal" button near the right-side "Add View" action.
- Support customizable terminal launch actions, e.g. open terminal from a local folder or from a specific saved node/path.
