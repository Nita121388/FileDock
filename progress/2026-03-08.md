# 2026-03-08

## P7 desktop sidecar build/check cleanup

- Removed the need for temporary placeholder sidecar binaries during local desktop Rust checks.
- Updated `apps/desktop/src-tauri/build.rs` to detect missing host-sidecar binaries in non-release profiles and temporarily strip Tauri `bundle.externalBin` during build-script execution.
- Kept release packaging strict: real sidecars are still required for `npm run tauri build`, so packaged apps continue bundling `filedock`, `filedock-sftp`, and `filedock-ssh` intentionally.
- Added the matching build-time `serde_json` dependency used for the temporary Tauri config patch merge.
- Updated desktop sidecar docs (`docs/desktop.md`, `apps/desktop/src-tauri/binaries/README.md`) to document the new dev/check behavior vs release packaging expectations.
- Updated `docs/roadmap.md` to move the recently finished desktop UX items out of the near-term backlog and summarize them under implemented desktop capabilities.

## Validation

- `cargo check -p filedock`
- `cargo check -p filedock-desktop`
- `cd apps/desktop && npm run build`
- `FILEDOCK_SMOKE_PORT=8788 ./scripts/smoke.sh`


## P8 desktop unit-test pass + release gating

- Added a desktop Vitest test runner (`apps/desktop/vitest.config.ts`) plus `npm test` / `npm run test:watch` scripts.
- Added focused unit coverage for this desktop milestone:
  - saved node / terminal preset parsing and persistence (`apps/desktop/src/ui/model/settings.test.ts`)
  - saved-node matching, terminal preset naming, imported server config parsing, and service-status error classification (`apps/desktop/src/ui/model/presetUtils.test.ts`)
  - terminal preset extraction/building (`apps/desktop/src/ui/model/terminalPresets.test.ts`)
  - HTTP client request/error/progress handling (`apps/desktop/src/ui/api/client.test.ts`)
  - pane table width persistence helpers (`apps/desktop/src/ui/components/panes/usePaneTableColumns.test.ts`)
  - byte formatting helper (`apps/desktop/src/ui/util/formatBytes.test.ts`)
- Refactored App-only preset helpers into `apps/desktop/src/ui/model/presetUtils.ts` so the new behavior is directly unit-testable.
- Exported the pane width/storage helper functions from `usePaneTableColumns.ts` for targeted tests instead of trying to snapshot UI interactions.
- Added Rust unit tests for HTTP error-body summarization in both the CLI and desktop Tauri backend (`crates/client_cli/src/main.rs`, `apps/desktop/src-tauri/src/main.rs`).
- Updated CI and release workflows to run desktop unit tests before desktop build / Tauri packaging.
- Updated release docs (`docs/release.md`) so local release steps now include `npm test` before packaging.

## Validation

- `cd apps/desktop && npm test`
- `cd apps/desktop && npm run build`
- `cargo test --workspace`
- `FILEDOCK_SMOKE_PORT=18787 ./scripts/smoke.sh`

## P9 release docker fix

- Fixed the release docker publish root cause: the repo was ignoring the workspace `Cargo.lock`, but `deploy/Dockerfile` explicitly copies it during image build.
- Stopped ignoring the root `Cargo.lock` so release tags and GitHub Actions checkout contexts include the lockfile needed by Docker builds.
- Plan after this fix: commit the lockfile itself, re-run local validation, then cut a fresh release so the full release workflow can go green (including GHCR image publish).

## Validation

- `cargo test --workspace --locked`
- `cargo build --release --locked -p filedock-server`
- GitHub Actions release run `22819106597` (tag `v0.2.4`) completed successfully, including the `docker` publish job.

## P10 v0.2.4 release published successfully

- Cut and pushed tag `v0.2.4` after tracking the workspace `Cargo.lock` in Git.
- GitHub Actions release workflow completed green across desktop builds, GitHub Release asset upload, and GHCR Docker publish.
- Published release assets now include Linux/macOS/Windows bundles plus `SHA256SUMS`.
- The Docker image push now succeeds from `deploy/Dockerfile`, confirming the missing-lockfile release failure is resolved.

## Validation

- GitHub Actions CI run `22819106250` completed successfully.
- GitHub Actions release run `22819106597` completed successfully.
- GitHub Release `v0.2.4` is published with downloadable assets.
