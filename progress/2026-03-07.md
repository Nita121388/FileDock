# 2026-03-07

## P0 / P1 follow-up

- Investigated the reported delete-with-backup failure where `push-folder` hit `create snapshot` with HTTP `502`.
- Local reproduction did **not** reproduce the `502`:
  - manual `push-folder` to a local server worked
  - `scripts/smoke.sh` passed end-to-end on `127.0.0.1:8788`
- Conclusion so far: the reported `502` is likely environment-specific (service/proxy/storage path) rather than a deterministic local logic failure.

## Changes shipped

- Improved HTTP error surfacing for snapshot creation / manifest upload in the Rust CLI (`crates/client_cli/src/main.rs`).
  - Server status code and trimmed response body are now preserved in error text.
- Improved the desktop Tauri backend snapshot upload path (`apps/desktop/src-tauri/src/main.rs`).
  - File backup path now preserves server response bodies for create/upload/manifest failures.
- Improved desktop web-client HTTP error messages (`apps/desktop/src/ui/api/client.ts`).
- Added FileDock service status indicator to the desktop top bar (`apps/desktop/src/ui/App.tsx`, `apps/desktop/src/ui/styles.css`).
  - States: checking / online / offline / error
  - Displays version when `/health` succeeds

## Validation

- `cargo check -p filedock`
- `cargo check -p filedock-desktop` (validated with temporary local sidecar placeholders; removed after check)
- `cd apps/desktop && npm run build`
- `FILEDOCK_SMOKE_PORT=8788 ./scripts/smoke.sh`

## P2 local browser right-click backup

- Added a lightweight right-click context menu in the desktop local file browser (`apps/desktop/src/ui/components/panes/local/LocalBrowserPane.tsx`).
- Local rows now support quick actions without opening the button strip:
  - `Backup to FileDock`
  - `Copy path`
- The active row is visually highlighted while the context menu is open (`apps/desktop/src/ui/styles.css`).
- Menu auto-dismisses on outside click, `Esc`, resize, blur, scroll, path changes, and when a backup starts.
- Menu items are disabled while loading to avoid repeated triggers.
- Added matching i18n strings in both desktop locale files:
  - `apps/desktop/src/ui/i18n/locales/en.json`
  - `apps/desktop/src/ui/i18n/locales/zh-CN.json`

## Validation (right-click backup step)

- `cd apps/desktop && npm run build`

## P3 saved node presets

- Added desktop saved node presets for connection configs (`apps/desktop/src/ui/model/settings.ts`, `apps/desktop/src/ui/App.tsx`).
- Users can now save the current connection under a required custom node name from the top bar, then re-apply it from a dropdown for quick switching.
- Saved presets persist in desktop settings and include:
  - server base URL
  - token
  - device id
  - device token
- Added help text plus i18n strings for preset save/apply flows in both desktop locale files.
- Kept the first version intentionally small and low-risk: save + dropdown apply, without adding extra preset management UI yet.

## Validation (saved node preset step)

- `cd apps/desktop && npm run build`


## P4 icon buttons + workspace terminal shortcut

- Added a shared desktop SVG icon component for consistent action buttons (`apps/desktop/src/ui/components/Icon.tsx`).
- Replaced several high-frequency plain text buttons with icon + tooltip interactions:
  - desktop top bar: save node preset, connection help, preferences, theme toggle, new workspace
  - workspace rail: add view, add terminal
  - local browser toolbar + row actions
  - SFTP browser toolbar + row actions
- Added a dedicated desktop "Add terminal" action next to "Add view" in the workspace tools rail.
- Kept labels/tooltip strings via existing i18n titles; added `workspace.tools.addTerminalTitle` in both locales.

## Validation (icon button pass)

- `cd apps/desktop && npm run build`

## P5 file list column resize

- Added draggable file-list column resizing for the desktop local and SFTP browser tables.
- Introduced a shared pane table column hook (`apps/desktop/src/ui/components/panes/usePaneTableColumns.ts`) that:
  - tracks column widths per pane type in `localStorage`
  - resizes adjacent columns with pointer drag
  - preserves minimum widths for name / type / size / actions columns
- Updated both table headers to expose resize handles:
  - `apps/desktop/src/ui/components/panes/local/LocalBrowserPane.tsx`
  - `apps/desktop/src/ui/components/panes/sftp/SftpBrowserPane.tsx`
- Updated shared desktop table styles for resizable headers and horizontal overflow safety (`apps/desktop/src/ui/styles.css`).
- Added localized resize-hint strings in both desktop locale files.

## Validation (file list column resize)

- `cd apps/desktop && npm run build`

## P6 terminal preset + custom launch flow

- Added persisted saved terminal presets in desktop settings (`apps/desktop/src/ui/model/settings.ts`).
  - Presets support both local and SFTP terminal targets.
  - SFTP presets keep the current host/auth/base-path fields so they can reopen the same remote terminal target later.
- Added shared terminal preset helpers (`apps/desktop/src/ui/model/terminalPresets.ts`) so local folders, SFTP paths, and saved presets all open terminals through the same tab builder.
- Added saved terminal preset controls to the desktop top bar (`apps/desktop/src/ui/App.tsx`).
  - Users can save the current active local folder / SFTP pane / terminal as a named preset.
  - Users can reopen saved terminal presets directly from a dropdown.
- Added local terminal quick actions (`apps/desktop/src/ui/components/panes/local/LocalBrowserPane.tsx`).
  - Toolbar button: open terminal in the current folder.
  - Row action + right-click menu: open terminal at the clicked folder (or parent folder for files).
- Added SFTP terminal quick actions (`apps/desktop/src/ui/components/panes/sftp/SftpBrowserPane.tsx`, `apps/desktop/src/ui/components/layout/LeafPane.tsx`).
  - Toolbar button still opens the current remote path.
  - Row action button opens a terminal at the clicked remote folder (or current directory for files).
  - Pane-header SFTP terminal action now shares the same terminal-tab builder.
- Added matching locale/help strings in both desktop locale files.

## Validation (terminal preset step)

- `cd apps/desktop && npm run build`
